rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permite acceso a usuarios autenticados (incluye login anonimo).
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
